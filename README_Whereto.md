Frontend edits → frontend/src/ (React/Vite code).
Backend edits → backend/app/ (Laravel models, controllers, migrations).
Database schema → backend/database/migrations (Laravel migration files).

-> UI changes → edit frontend.
-> Business logic → edit backend.
-> Schema changes → add new migration in backend.